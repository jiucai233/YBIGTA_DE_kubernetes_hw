# ==========================================
# The Great Slim-Down Challenge
# Your Goal: Build an image < 150MB
# ==========================================

# Stage 1: The Builder
# Hint: Start with a slim python image (e.g., python:3.12-slim) as 'builder'
# Hint: Can you use a tool like 'uv' or 'venv' to install dependencies efficiently?
# Hint: Do you really need to install `build-essential` or `cmake` here?

# [YOUR BUILDER STAGE CODE HERE...]


# Stage 2: The Runtime (Light)
# Hint: Start fresh with another ultra-slim python image
# Hint: Copy ONLY what is strictly necessary from the builder stage
# Hint: Copy the `server.py` file from the baseline/ directory into your container
# Hint: Ensure your ENTRYPOINT runs the server on port 8080!

# [YOUR RUNTIME STAGE CODE HERE...]